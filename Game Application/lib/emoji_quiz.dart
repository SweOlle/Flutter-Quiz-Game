import 'package:flutter/material.dart';

class EmojiQuiz extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.deepPurple[300],
      ),
      backgroundColor: Colors.deepPurple[300],
      body: Center(child: Text('Content of Mini Game 1')),
    );
  }
}

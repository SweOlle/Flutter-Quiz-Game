import 'package:flutter/material.dart';

class CharadesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Charades')),
      body: Center(child: Text('Content of Mini Game 1')),
    );
  }
}

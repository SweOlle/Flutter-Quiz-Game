import 'package:flutter/material.dart';

class TruthorDarePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Truth or Dare')),
      body: Center(child: Text('Content of Mini Game 1')),
    );
  }
}
